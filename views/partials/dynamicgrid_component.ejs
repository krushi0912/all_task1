<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        .container {
            display: flex;
            float: right;
        }

        .container i {
            padding: 10px;
            cursor: pointer;
            border: 2px solid rgb(188, 185, 185);
            border-radius: 10px;
            margin: 5px;
            background-color: rgb(221, 243, 250);
            color: grey;
        }

        .page {
            padding: 10px 10px;
            margin: 5px;
            border: 2px solid rgb(188, 185, 185);
            border-radius: 10px;

        }
    </style>
</head>

<body>
    <div class="container">
        <%if(Number(p)!=1){ %>
        <a href="/pagination/dynamic_grid?page=1&textquery=<%=sql%>"><i
                    class="fa-solid fa-angles-left"></i></a>
        <a href="/pagination/dynamic_grid?page=<%=Number(p)-1 %>&textquery=<%=sql%>"><i
                    class="fa-solid fa-angle-left"></i></a>
        <%}%>
        <%if(lastpage != 1){%>
        <div class="page">
            <%=p%>
        </div>
        <%}%>
        <%if(Number(p)!=lastpage){%>
        <a href="/pagination/dynamic_grid?page=<%=Number(p)+1 %>&textquery=<%=sql%>"><i
                    class="fa-solid fa-angle-right"></i></a>
        <a href="/pagination/dynamic_grid?page=<%=lastpage%>&textquery=<%=sql%>"><i
                    class="fa-solid fa-angles-right"></i></a>
        <%}%>
    </div>
</body>

</html>